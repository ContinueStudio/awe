# 2026-07-07

## PRD v3.0 差异分析
- 全面审查了前后端全部代码，与 PRD v3.0 逐项对比
- 生成差异报告：`docs/PRD_GAP_ANALYSIS.md`
- 当前完成度约 55%，共识别 27 个缺口，分 5 个阶段

## 阶段一开发完成 (v0.3.7)
- D-1: workflows 表增加 workspace_id/version/status，自动迁移兼容旧库
- D-2: 回收站机制（软删除→restore→permanent delete），API + DB 全链路
- D-3: 前端 TrashPage + LeftNav 回收站入口
- D-4: crypto.py Fernet 加密 + settings 表 + .env 主密钥
- D-5: SettingsPage LiteLLM API Key 录入表单（OpenAI/Anthropic/Google）
- D-6: sandbox.py sys.addaudithook 底层拦截（exec/Popen/os.system/socket/文件写入）
- 全部 6/6 底层测试 + 13/13 API 烟测通过
- Git commit: bf8ea0a → push to main
- 避坑：FastAPI `/workflows/trash` 必须在 `/{wid}` 之前声明；Node22 需用 npx.cmd 调 vite

## 第二次紧急修复 (14:47)
- App.tsx 从 37e29e9 恢复完整版本（370+ 行代码被误删）
- 恢复功能：框选模式、运行历史、画布多选、关闭确认、节点剪贴板、错误高亮、CustomTitleBar
- 重新添加 TrashPage 路由 + RunHistoryDrawer onRun 属性 + v0.3.7 版本号
- 提交 47965bc → push to main
- 避坑：修改已有大量功能的文件时，不能用简化版本直接覆盖

## 紧急修复 (13:45 → 14:05)
- HomePage.tsx 被误删多选/批量删除/确认对话框/Toast — `git checkout HEAD --` 恢复
- RunHistoryDrawer shadow-2xl 在 translate-x-full 时溢出右侧黑影 — `opacity-0 pointer-events-none overflow-hidden` 修复
- 构建验证：dist 包含"多选"+"已选"+"opacity-0 pointer-events-none" 标记
- Vite build: 1594 modules → 0 errors, hash DbCzVL-r
- 提交 7188d2e → push to main
- 注意：工作区中还有 BottomToolbar/Canvas/ConfirmDialog/NodePanel/NodeRender/ui.css/index.css/tailwind.config.js 等未提交修改，非本次引入
