# 2026-07-06 Daily Log

## v0.3.12 — 左工具栏重新设计为 6 图标布局
- **重新设计**：按指定规格重写左工具栏，6 个图标 + 1 条分割线
  - 左1 圆形加号（深灰 #4B5563 实心圆 + 白色十字）→ 新建节点
  - 左2 带加号文档 → 添加注释
  - 分割线 #E5E7EB 在左2/左3之间
  - 左3 鼠标指针选择工具 → 激活蓝底 #DBEAFE + 蓝图标 #2563EB
  - 左4 手掌拖拽工具 → 默认激活（#DBEAFE + #2563EB），与选择模式互斥
  - 左5 四宫格加号 → 自动排版
  - 左6 三点更多 → 弹出菜单
- **移除**：旧的 5 小工具按钮 + 蓝色"添加节点"大字按钮
- **变更文件**: BottomToolbar.tsx 完全重写
- **验证**: npm run build 通过

## v0.3.11 — 节点简介 / 单节点运行 / 开始节点 / 框选运行
- **Feat 1 节点简介**: 执行日志每条记录增加 `desc` 字段（NodeDefinition.description），RunHistoryDrawer 日志行下方灰色斜体显示节点简介
- **Feat 2 单节点运行**: NodeConfigDrawer 的「试运行」按钮改为调用 `POST /api/workflows/{wid}/nodes/{nid}/run`，仅执行当前节点逻辑，构建单节点临时图独立运行
- **Feat 3 开始节点**: registry 新增 `start` 类型，作为工作流入口（透传输入给下游），拓扑序保证未连接 start 下游的节点不被执行
- **Feat 4 框选运行**: 画布通过选择模式或 Shift+拖动框选 2 个及以上节点后，底部工具栏出现绿色「运行选中 (N)」按钮，调用 `POST /api/workflows/{wid}/run-selected` 仅执行子图
- **变更文件**: registry.py / builder.py / workflows.py / api.ts / App.tsx / Canvas.tsx / BottomToolbar.tsx / RunHistoryDrawer.tsx / types.ts
- **验证**: py_compile + npm run build 通过


- **Feat 1**: 底部工具栏新增「选择」按钮（小图标），点击切换画布交互模式
  - 选择模式下，点击空白画布进入框选（矩形多选节点）
  - 非选择模式下，点击空白画布平移（默认行为）
  - 按钮激活态蓝底白字，非激活态透明
- **Feat 2**: 底部工具栏新增「日志」按钮（小图标），点击弹出 RunHistoryDrawer
  - 基于工作流 id 读取运行历史，可查看 logs/outputs/error
- **变更文件**: Canvas.tsx（selectMode prop）、BottomToolbar.tsx（两个新按钮）、App.tsx（状态管理 + 抽屉）
- **验证**: npm run build 通过
- **修正**: RunHistoryDrawer 从右侧全高抽屉改为左侧悬浮面板（与右侧 ConfigPanel 对称）
  - 去掉了 backdrop 遮罩和 `translate-x-full` 滑入动效
  - 改为 `fixed left-4` 悬浮卡片（白底 + 圆角 + 阴影），和 ConfigPanel 一致的风格
- **全局规则**: 写入 ~/.workbuddy/MEMORY.md「AWE 项目操作铁律」——修改代码后必须自动重启再汇报

## v0.3.10a — 去掉运行按钮 + 修复节点重叠穿透
- **Bug 1**: 运行历史面板右上角有「运行」按钮，用户要求去掉
  - 修复: RunHistoryDrawer.tsx 去掉运行按钮，同时删除 `onRun` prop（App.tsx 和 HomePage.tsx 同步清理）
- **Bug 2**: 节点重叠时背后节点文字穿透显示
  - 根因: `.node-card` 背景是 `linear-gradient` 从 `var(--node-color)` 渐变到 `#ffffff`，而 `--node-color` 被设为 `color + "20"`（12% alpha），导致顶部半透明
  - 修复: `index.css` 把 `.node-card` 背景改为实底白 `#ffffff` + `border-top: 3px solid var(--node-color)`；NodeRender.tsx 去掉 `--node-color` 的 `20` alpha 后缀，让类型色完全 opaque
- **验证**: npm run build 通过，后端已自动重启

## v0.3.9 — 新增飞书读取所有记录节点 + 实际创建了工作流
- **Feat**: 新增 `feishu_bitable_list_records` 节点（自动分页读取所有记录）
  - `feishu_client.py` 加 `_request_get` 方法 + `list_records` 函数
  - `registry.py` 加节点定义，输出 records（数组）+ total（数量）
  - `builder.py` 加分派 + handler
- **工作流已创建**: 通过 `POST /api/workflows` 创建了「飞书读取所有记录并打印」（id: 1f2bff6d）
  - n1: feishu_bitable_list_records（app_token/table_id 已预填，app_id/secret 留空）
  - n2: skill 沙盒（循环逐条打印每条记录的 record_id 和所有字段）
  - 边: n1 → n2
- **canvas 修复**: 
  - 连线不能取消 → 按 Escape 或点击空白画布取消拖拽中的连线
  - 连线不能删除 → 点击连线高亮选中（变蓝），按 Delete 或 Backspace 删除
  - skill 节点无输入端口导致 n1→n2 的边不渲染 → 给 skill 加了 `data` 输入端口
  - 点击节点/连线/画布时清除另一方的选中态
- **工具**: `seed_feishu_read.py` 同步修复了 edge 格式
- **验证**: py_compile 通过，npm run build 通过，注册表 16 个节点

- **Feat**: 新增 3 个飞书多维表格工作流节点，支持在工作流中创建多维表格、新增字段、写入记录
- **实现**:
  - `backend/app/engine/feishu_client.py` — 飞书 Open API 客户端封装（token 获取 + 自动缓存刷新、创建 bitable、创建字段、创建记录）
  - `backend/app/nodes/registry.py` — 新增 3 个 NodeDefinition (feishu_bitable_create / field / record)
  - `backend/app/engine/builder.py` — 新增 3 个 async handler 和 elif 分派
- **节点说明**:
  - **飞书-创建多维表格**: 接收 app_id/app_secret/name，返回 app_token/table_id/url
  - **飞书-新增字段**: 接收 app_token/table_id + 字段配置，支持文本/数字/单选/多选/日期
  - **飞书-新增记录**: 接收 app_token/table_id + 字段值 dict，写入一条记录
- **验证**: py_compile 通过，npm run build 通过，注册表包含 15 个节点
- **启动**: 重启 start.bat 即可生效，前端自动识别新节点类型
